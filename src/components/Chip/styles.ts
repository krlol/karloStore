import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    chip:{
        paddingHorizontal: 8,
        paddingVertical: 4,
        borderRadius: 8,
    },
    label:{
        textAlign: 'center',
        fontSize: 12,
        fontWeight: 'bold',
    }
})

export default styles;