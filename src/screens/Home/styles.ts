import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container:{
        paddingTop: 25,
        marginHorizontal: 15,
    },
    headerContainer:{

    },
    chipsContainer:{
        flexDirection: 'row',
        marginVertical: 10,
    },
    productsContainer:{
        flexDirection: 'row',

    }
})

export default styles;