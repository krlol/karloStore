import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    scrollView:{
        paddingTop: 25,
        marginHorizontal: 15,
    },
    headerContainer:{

    },
    chipsContainer:{
        flexDirection: 'row',
        marginVertical: 10,
    }
})

export default styles;